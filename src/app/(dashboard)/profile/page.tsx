import ContentLayout from "@/components/layout/content-layout";

export default function Profile({}) {

    return (
        <ContentLayout>
            <h1>Profile</h1>
        </ContentLayout>
    )
}